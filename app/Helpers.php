<?php

namespace App\Helpers;

class Helper {

    public static function slugUrl($sEngTitle, $sBngTitle) : array{
        //Create SLUG Url
        // try{
        //     $sEn = strtolower($sEngTitle);
        //     $sBn = strtolower($sBngTitle);

        //     $sEnUrl = preg_replace('/\s+/', '-', $sEn);
        //     $sBnUrl = preg_replace('/\s+/', '-', $sBn);
        
        //     return ['en_slug'=>$sEnUrl,'bn_slug'=>$sBnUrl];
        // }catch(\Exception $e){
        //     return $e->getMessage();
        // }
    }
}